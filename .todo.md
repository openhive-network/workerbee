# Python Branch Review Follow-ups

Scope: current `kmochocki/python-snake-case` branch after multi-agent review on
2026-07-02. TypeScript remains the source of truth for behavior and
architecture unless a Python divergence is explicitly accepted and documented.

## Medium

- Add a concurrency/backpressure strategy for observer delivery.
  - Evidence: `python/workerbee/chain_observers/observer_mediator.py:70`
    schedules listener tasks without awaiting them, `python/workerbee/chain_observers/bot.py:248`
    keeps polling, and async iterator queues default to unbounded at
    `python/workerbee/chain_observers/queen.py:149`.
  - Needed: decide whether to bound per-listener tasks, expose a queue limit, or
    document the no-backpressure model.

- Decide composite-filter cancellation parity.
  - Evidence: Python cancels losing operands at
    `python/workerbee/chain_observers/filters/composite_filter.py:41` and
    `python/workerbee/chain_observers/filters/composite_filter.py:86`;
    TypeScript races promises without cancellation at
    `src/chain-observers/filters/composite-filter.ts:39`.
  - Needed: either keep cancellation as an accepted Python improvement or match
    TS so losing stateful filters can still finish.

- Decide multi-account state handling for stateful filters.
  - Evidence: Python stores prior balance/metadata per account at
    `python/workerbee/chain_observers/filters/balance_change_filter.py:29` and
    `python/workerbee/chain_observers/filters/account_metadata_change_filter.py:25`;
    TS stores one previous value per filter at
    `src/chain-observers/filters/balance-change-filter.ts:34` and
    `src/chain-observers/filters/account-metadata-change-filter.ts:28`.
  - Needed: keep as an intentional Python bugfix with TS backlog, or align to
    TS if strict parity wins.

- Tighten classifier/result typing instead of relying on `Any` and casts.
  - Evidence: typed shapes exist in
    `python/workerbee/chain_observers/classifier_results.py`, but
    `DataEvaluationContext.get()` casts collector output at
    `python/workerbee/chain_observers/factories/data_evaluation_context.py:81`,
    while base and JSON-RPC collectors still return `dict[str, Any]`, e.g.
    `python/workerbee/chain_observers/collectors/collector_base.py:36`,
    `python/workerbee/chain_observers/collectors/jsonrpc/account_collector.py:58`,
    and `python/workerbee/chain_observers/collectors/jsonrpc/block_collector.py:34`.
  - Needed: propagate classifier result aliases through collectors and tests so
    mypy catches missing keys and payload drift.

- Make required option dictionaries fail fast.
  - Evidence: missing option keys are silently ignored or defaulted at
    `python/workerbee/chain_observers/collectors/set_managed_collector.py:28`,
    `python/workerbee/chain_observers/collectors/common/manabar_collector.py:49`,
    and `python/workerbee/chain_observers/collectors/jsonrpc/content_metadata_collector.py:106`.
  - Needed: replace defaults with required-key access or explicit validation at
    registration/query boundaries.

- Add missing operation variant tests.
  - Evidence: code supports `account_create_with_delegation_operation` and
    `limit_order_create2_operation` at
    `python/workerbee/chain_observers/filters/account_created_filter.py:32`
    and `python/workerbee/chain_observers/providers/internal_market_provider.py:88`,
    but tests mostly cover older variants at `python/tests/unit/test_filters.py:270`,
    `python/tests/unit/test_filters.py:386`, `python/tests/unit/test_providers.py:412`,
    and `python/tests/unit/test_providers.py:640`. `transfer_from_savings_operation`
    has filter coverage at `python/tests/unit/test_filters.py:1565`, but provider
    tests cover only transfer/recurrent/escrow at
    `python/tests/unit/test_providers.py:507` and
    `python/tests/unit/test_providers.py:583`.
  - Needed: add provider/filter parity tests for these variants.

- Fix false-green and low-value tests.
  - Evidence: `python/tests/unit/test_alarm_provider.py:128` uses a stale
    "soon" date that is now in the past and only checks membership at
    `python/tests/unit/test_alarm_provider.py:137`. `python/tests/integration/test_provider_data.py:51`
    accepts placeholder RC manabar data. Schema/constant-only checks remain at
    `python/tests/unit/test_alarm_provider.py:267`,
    `python/tests/unit/test_alarm_provider.py:283`,
    `python/tests/unit/test_manabar_collector.py:478`,
    `python/tests/unit/test_account_family_collectors.py:282`, and
    `python/tests/unit/test_content_metadata.py:695`.
  - Needed: assert exact alarm categories, require meaningful RC manabar values,
    and remove or replace tests that only check constants or result shape.

- Add direct async `on_next` failure parity coverage.
  - Evidence: TS covers async `next()` failure at
    `__tests__/detailed/bot_events.ts:226`; Python integration currently checks
    provider pipeline error routing at `python/tests/integration/test_subscription.py:135`.
  - Needed: add a direct async callback failure case and verify the expected
    routing behavior.

- Move or rename internal-heavy Queen/API tests that do not mirror public TS
  usage.
  - Evidence: Python callback/`close()` API appears at
    `python/workerbee/chain_observers/queen.py:82` and
    `python/workerbee/chain_observers/queen.py:641`, while TS exposes observer
    unsubscribe at `src/queen.ts:112`; private mediator assertions remain at
    `python/tests/unit/test_queen.py:218`, `python/tests/unit/test_queen.py:270`,
    `python/tests/unit/test_queen.py:424`, and `python/tests/unit/test_queen.py:637`.
  - Needed: prefer public behavior tests, or mark the remaining tests as
    Python-only API coverage.

- Add AccountCollector chunking coverage and stronger value assertions.
  - Evidence: chunking tests live at
    `python/tests/unit/test_jsonrpc_collector_chunking.py:104` but omit
    `AccountCollector`, whose chunking path is at
    `python/workerbee/chain_observers/collectors/jsonrpc/account_collector.py:65`.
  - Needed: cover account chunking and assert transformed values, not just
    request sizes/counts.

## Low

- Decide provider merge behavior for `None`.
  - Evidence: Python drops provider keys with `None` at
    `python/workerbee/chain_observers/observer_mediator.py:110`; TS drops only
    `undefined`, so `null` is delivered at `src/chain-observers/observer-mediator.ts:65`.
  - Needed: either keep Python `None` as "no payload" by design or preserve the
    key to mirror TS `null`.

- Remove misleading integer custom-operation IDs or support them fully.
  - Evidence: `python/workerbee/chain_observers/queen.py:388` accepts `str | int`,
    but `python/workerbee/chain_observers/providers/custom_operation_provider.py:19`
    and `python/workerbee/chain_observers/providers/custom_operation_provider.py:43`
    operate on string operation IDs.
  - Needed: narrow the public type to `str` or add coherent int handling and tests.

- Remove or repair unused unsafe classifier unregistration API.
  - Evidence: `python/workerbee/chain_observers/factories/factory_base.py:64`
    can delete a shared collector instance while another classifier still maps
    to the collector class, e.g. `python/workerbee/chain_observers/factories/historydata/factory.py:39`.
  - Needed: remove the unused API or make shared-collector unregister safe.

- Replace loose local content metadata aliases with public typed shapes.
  - Evidence: `python/workerbee/chain_observers/collectors/jsonrpc/content_metadata_collector.py:25`
    defines `ContentMetadata = dict[str, object]` while public shapes exist in
    `python/workerbee/chain_observers/payloads.py:443` and
    `python/workerbee/chain_observers/classifier_results.py:89`.
  - Needed: reuse the public aliases so metadata field drift is type-checked.

- Keep Python-specific lifecycle tests only if explicitly accepted.
  - Evidence: Python-only `aclose`, async context manager, and `run_forever`
    tests live at `python/tests/unit/test_bot.py:131`,
    `python/tests/unit/test_bot.py:264`, `python/tests/unit/test_bot.py:337`,
    and `python/tests/integration/test_lifecycle.py:43`.
  - Needed: mark/document these as Python API value-adds or reduce them if
    strict TS test-methodology parity is required.

- Fill public documentation/docstrings for exported Python API.
  - Evidence: exported public API lacks class/method docstrings in examples such
    as `python/workerbee/chain_observers/factories/factory_base.py:26`,
    `python/workerbee/chain_observers/collectors/collector_base.py:30`,
    `python/workerbee/chain_observers/enums.py:8`,
    `python/workerbee/chain_observers/errors.py:9`, and many exported
    filters/providers under `python/workerbee/chain_observers/filters/` and
    `python/workerbee/chain_observers/providers/`.
  - Needed: document public classes/methods or make internal-only classes
    private/non-exported where appropriate.

- Add docstrings or private prefixes for reusable test fixtures and example
  helpers.
  - Evidence: shared integration fixtures in
    `python/tests/integration/conftest.py:32`,
    `python/tests/integration/conftest.py:40`,
    `python/tests/integration/conftest.py:45`,
    `python/tests/integration/conftest.py:52`,
    `python/tests/integration/conftest.py:58`, and
    `python/tests/integration/conftest.py:64` lack docstrings. Example helper
    functions at `python/examples/account_observer.py:22`,
    `python/examples/block_parser.py:21`,
    `python/examples/custom_json_spotter.py:22`, and
    `python/examples/post_observer.py:55` are public-looking.
  - Needed: document reusable fixtures and either document or private-prefix
    example helpers.

- Keep Python CI variables scoped to Python jobs.
  - Evidence: `python/.gitlab-ci.yml:6` defines Python diagnostic variables at
    top-level, so they can leak into existing TypeScript jobs after root CI
    includes the Python CI file.
  - Needed: move Python-only variables under `.python_job` or individual
    `python_*` jobs.
